% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/document_targets.R
\name{view_tardoc_db}
\alias{view_tardoc_db}
\title{Open the Tier 3 server analytics viewer (Quack + optional LLM chat)}
\usage{
view_tardoc_db(
  project_path = ".",
  site_dir = "tardoc",
  port = 9000,
  quack_port = 9494,
  llm_chat = NULL,
  db_extensions = FALSE
)
}
\arguments{
\item{project_path}{Path to the targets project root.}

\item{site_dir}{Subfolder used in \code{\link[=document_targets]{document_targets()}}.}

\item{port}{Port for the HTML server. Default \code{9000}.}

\item{quack_port}{Port for the Quack DuckDB server. Default \code{9494}.}

\item{llm_chat}{An ellmer \code{Chat} object for the chat interface.
Any provider: \code{ellmer::chat_openai()}, \code{ellmer::chat_ollama("llama3.2")},
\code{ellmer::chat_anthropic()}, \code{ellmer::chat_openai_compatible(base_url)}.
\code{NULL} (default) disables chat.}

\item{db_extensions}{Logical. Install community DuckDB extensions when
building \code{tardoc.duckdb}: \code{quackformers} (BERT semantic search), \code{faiss},
\code{duckdb_mcp}. Slow on first run. Default
\code{FALSE}.}
}
\description{
Starts a DuckDB Quack server (background process via \code{callr}) and a minimal
httpuv HTML server. The browser DuckDB WASM connects via Quack for all
queries. When \code{llm_chat} is provided, a \verb{/chat} endpoint is exposed and the
Chat tab becomes active.
}
