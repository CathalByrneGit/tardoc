% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dag_layout.R
\name{dag_layout}
\alias{dag_layout}
\title{Assign layered (x, y) positions to a DAG}
\usage{
dag_layout(nodes, edges, x_spacing = 220, y_spacing = 90)
}
\arguments{
\item{nodes}{Character vector of node names.}

\item{edges}{A data frame or list with \code{from} and \code{to} character columns.}

\item{x_spacing}{Horizontal distance between layers, in pixels.}

\item{y_spacing}{Vertical distance between stacked nodes, in pixels.}
}
\value{
A data frame with \code{name}, \code{layer}, \code{x} and \code{y}.
}
\description{
Computes a left-to-right layered layout: each node's column is its
longest-path depth from a root, and nodes sharing a column are stacked
vertically and centred against the tallest column.
}
\details{
Cycles cannot occur in a \code{targets} pipeline, but a malformed edge list could
contain one. Rather than looping forever, any node still unresolved once no
further progress is possible is placed in the last computed layer.
}
\examples{
dag_layout(
  nodes = c("a", "b", "c"),
  edges = data.frame(from = c("a", "b"), to = c("b", "c"))
)
}
