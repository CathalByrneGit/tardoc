% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/document_targets.R
\name{.await_quack}
\alias{.await_quack}
\title{Wait for the background Quack server to accept connections}
\usage{
.await_quack(proc, port, timeout = 15)
}
\arguments{
\item{proc}{A \code{callr} process, or \code{NULL}.}

\item{port}{Integer. The Quack port.}

\item{timeout}{Numeric. Seconds to wait before giving up.}
}
\value{
\code{proc} when the port is accepting connections, otherwise \code{NULL}
after reporting why -- the caller then falls back to JSON.
}
\description{
Polls the port rather than sleeping a fixed interval and checking
\code{is_alive()}. That check was a race: \code{quack_serve()} returns immediately and
the process used to exit right behind it, so liveness at an arbitrary moment
said nothing about whether the browser would be able to attach. A successful
TCP connect does.
}
\keyword{internal}
