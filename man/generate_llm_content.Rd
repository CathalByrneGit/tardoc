% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_llm_content.R
\name{generate_llm_content}
\alias{generate_llm_content}
\title{Generate LLM content for undescribed targets and all functions}
\usage{
generate_llm_content(
  targets_data,
  function_names,
  cfg,
  llm_chat = NULL,
  provider = "openai",
  model = NULL,
  api_key = NULL,
  base_url = NULL
)
}
\arguments{
\item{targets_data}{Output of \code{\link[=load_targets_data]{load_targets_data()}}.}

\item{function_names}{Character vector of function names to explain.}

\item{cfg}{A site config list.}

\item{llm_chat}{An existing \code{ellmer} Chat object. When supplied,
\code{provider}, \code{model}, \code{api_key} and \code{base_url} are ignored.}

\item{provider}{Character. One of \code{"openai"}, \code{"anthropic"},
\code{"ollama"}, or \code{"openai_compatible"}. Default \code{"openai"}.}

\item{model}{Character. Model name. Defaults to the provider's own
default when \code{NULL}.}

\item{api_key}{Character. API key. When \code{NULL} the provider's
standard environment variable is used.}

\item{base_url}{Character. Base URL for \code{"ollama"} and
\code{"openai_compatible"} providers.}
}
\value{
Invisibly \code{NULL}; called for its side effect of rewriting the
generated blocks of the target and function markdown pages.
}
\description{
Fills in descriptions for targets that have none and adds a plain-language
explanation to each function page. Requires the \code{ellmer} package.
}
