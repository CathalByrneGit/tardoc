% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_targets_data.R
\name{load_targets_data}
\alias{load_targets_data}
\title{Load all targets project data}
\usage{
load_targets_data(cfg)
}
\arguments{
\item{cfg}{A site config list produced by \code{\link[=build_site_config]{build_site_config()}}.}
}
\value{
A named list with:
\describe{
\item{meta}{Tibble of target metadata. Status/time columns are \code{NA} if
no store exists.}
\item{target_names}{Character vector of stem/pattern target names.}
\item{network}{List from \code{\link[targets:tar_network]{targets::tar_network()}}.}
\item{manifest}{Tibble from \code{\link[targets:tar_manifest]{targets::tar_manifest()}}.}
\item{has_store}{Logical. Whether a store was found.}
}
}
\description{
\code{tar_manifest()} and \code{tar_network()} only require \verb{_targets.R} to exist --
they parse the pipeline definition without needing a store. \code{tar_meta()}
requires the store (i.e. the pipeline has been run at least once) and
provides run history: status, timestamps, and errors. If no store is found
\code{meta} is filled with \code{NA}s so all downstream functions still work.
}
