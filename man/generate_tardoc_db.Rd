% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_tardoc_db.R
\name{generate_tardoc_db}
\alias{generate_tardoc_db}
\title{Generate the tardoc DuckDB analytics database}
\usage{
generate_tardoc_db(targets_data, function_names, cfg, db_extensions = FALSE)
}
\arguments{
\item{targets_data}{Output of \code{\link[=load_targets_data]{load_targets_data()}}.}

\item{function_names}{Character vector of function names.}

\item{cfg}{A site config list.}

\item{db_extensions}{Logical. When \code{TRUE}, attempts to install community
extensions: \code{quackformers} (BERT embeddings), \code{faiss} (ANN index),
\code{duckdb_mcp}
(MCP server config). Each step is wrapped in \code{tryCatch}; a failure skips
that layer and the reason is recorded in the \verb{_meta_detail} table.
Default \code{FALSE} -- core tables and FTS only.}
}
\value{
Path to \code{tardoc.duckdb} invisibly, or \code{NULL} if the \code{duckdb}
package is not installed.
}
\description{
Creates \code{tardoc/tardoc.duckdb}. Attempts each capability layer in turn;
skips cleanly if the required extension is unavailable. The \verb{_meta} table
records which capabilities succeeded so the analytics viewer can adapt its
UI accordingly.
}
