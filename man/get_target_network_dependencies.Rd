% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependency_tracker.R
\name{get_target_network_dependencies}
\alias{get_target_network_dependencies}
\title{Get upstream and downstream dependencies for a target}
\usage{
get_target_network_dependencies(
  target_name,
  network_data,
  max_depth_up = Inf,
  max_depth_down = Inf,
  visited_up = NULL,
  visited_down = NULL
)
}
\arguments{
\item{target_name}{Character. Name of the target to analyse.}

\item{network_data}{List from \code{\link[targets:tar_network]{targets::tar_network()}} with \code{vertices} and
\code{edges} data frames.}

\item{max_depth_up}{Integer. Max depth for upstream traversal. Use \code{Inf}
for the full graph.}

\item{max_depth_down}{Integer. Max depth for downstream traversal.}

\item{visited_up, visited_down}{Internal recursion tracking vectors -- leave
as \code{NULL} when calling directly.}
}
\value{
A list with:
\describe{
\item{upstream}{Character vector of upstream node names.}
\item{downstream}{Character vector of downstream node names.}
\item{edges}{Tibble of \code{from}/\code{to} edges traversed.}
}
}
\description{
Recursively retrieves both upstream and downstream dependencies of a target
with independently controlled depths, and returns relevant edges.
}
\examples{
network_data <- list(
  vertices = data.frame(name = c("A", "B", "C"), type = "target"),
  edges    = data.frame(from = c("A", "B"), to = c("B", "C"))
)
res <- get_target_network_dependencies("B", network_data,
                                       max_depth_up = 1, max_depth_down = 1)
print(res$edges)

}
